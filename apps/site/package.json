{
    "name": "site",
    "version": "0.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "clean": "rimraf dist",
        "dev": "vocs dev",
        "build": "vocs build -o dist",
        "preview": "vocs preview"
    },
    "devDependencies": {
        "@guidanoli/cmioc": "workspace:*",
        "rimraf": "^5.0.5",
        "vocs": "1.0.0-alpha.52"
    }
}
